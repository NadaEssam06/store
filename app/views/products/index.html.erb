<h1>Products#index</h1>
<%= link_to "New product", new_product_path if authenticated? %>
<div id="products">
    <% @products.each do |p|%>
    <div>
        <a href="/products/<%= p.id %>">
          <%= p.name %>
        </a>
    </div>

    <%end%>
</div>
<% debug @products%>